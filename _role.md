# Ledger: causalmap-site-prototype

Role block: top of `CLAUDE.md`. Convention: hub `CLAUDE.md` -> "Role block and ledger".

One line per scored task: date | criterion Claude set itself | self-score | reason | Steve's verdict (which overrides the self-score). `!assessment` and `bullets!` lines are Steve's explicit voice and weigh heavily. Score only where there is real signal, not every session.
